{
  "name": "hell-harbor-physical-media-shop",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:init:local": "wrangler d1 execute hell-harbor-media-shop --local --file=./schema.sql",
    "check": "node --check src/index.js && node --check src/catalog.js && node --check public/app.js"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
